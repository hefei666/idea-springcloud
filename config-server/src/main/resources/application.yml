
server:
  port: 8888
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com//hefei666//idea-springcloud
          searchPaths: config-server
          username: hefei666
          password:
      label: master

